
1. 将原来的“每次收到数据都插入新记录”改为“按5分钟时间窗口聚合存储”。✅
	1. 实现逻辑
		1. 时间对齐：将 eventTime 向下取整到5分钟边界（如 16:38:35 → 16:35:00）
		2. 存储策略：
			1. 基于（ deviceId + eventTime）
			2. 同一设备同一对齐时间的第一条数据 → 插入新记录
			3. 后续数据 → 更新已有记录（只更新非null字段，null值不覆盖）
		3. 技术实现
			1. 新增 EventTimeAligner：5分钟时间对齐工具
			2. 修改 EventTimeParser：解析失败返回null，触发消息重试
			3. 新增 saveOrUpdateWithNullIgnore：基于 (deviceId, eventTime) 的保存或更新，null值不覆盖
	
	2. 会有并发问题，需要处理：
		场景：同一窗口的多条消息并发到达
		消息A：eventTime=16:35:10 → 对齐到16:35:00
		消息B：eventTime=16:35:20 → 对齐到16:35:00
		两者可能同时尝试插入(device_id, 16:35:00)的记录
		解决方案：
			1. 使用数据库唯一索引(device_id, event_time)
			2.使用saveOrUpdate：先尝试插入，如果唯一索引冲突则更新，这是一种 **Upsert (Update or Insert)** 策略
1. 数据库表重定义
	1. 原则：站点/仓位是“物理资产”，设备是“可移动工具”，两者通过“安装关系（带生效时间区间）”关联；核心难点在于如何维护**时间维度**上的对应关系，以确保当我们查询“某个仓位的历史数据”时，能把不同时间段、由不同设备采集的数据串联起来。
	2. 模糊搜索：匹配的是站名和仓位名吧？
	3. 数据库表不要冗余存储，在device表中存储了site_id，不需要存储site_nam。否则站点名更改会导致不一致，通过id建立外键，join查询更规范。
	4. 在状态量表和模拟量以及文件表中，建议冗余的添加一个position_id，只需要保证数据写入的时候，根据当前的安装关系表，将position_id写入数据行。如果采用不冗余的方法， 在查询仓位历史数据的时候，需要去安装关系表查询该仓位在该时间段内所有的安装设备，然后拼接sql语句查询。
	5. 设备表中是否应该记录station_id和position_id？
		1. 理论上：不需要，设备和仓位的关系是多对多，设备表应该只描述设备本身的属性，设备安装在哪里应该去installation表中查询。
		2. 实践上：建议保留。查询所有设备时，不用去installation关联查询所有设备的最新状态。但是在安装和拆装的时候要同步更新device和installation表。
	6. device表中的id和device_id
		1. 在analog_data等表中用外键关联的是device_id，索引巨大，查询变慢，磁盘浪费。万一厂家修改编码， 还需要修改所有表中的几千万条数据。字符串比对比整数比对要慢。
		2. 保留id，数据表中把device_id改为device_ref_id,将device表中id作为数据表的外键。
	7. waveform_file
		1. 按照事件时间查询是否正确,eventtime一定是一致的对吗？
	 
 你是一个资深的开发工程师，现在这个项目是你实现的。经过验收我提出一些你需要修改的地方，你先不要急着开发，先和我讨论清晰我们的需求。

当前我们的逻辑是一个设备安装在一个站名下的某个开关上，这是长期固定的。但是实际使用的情况是，”设备”是指用来监测数据的工具，“站点”是一个物理意义上的配电站或变电站房，“仓位”是指站点内可以被监测数据的地方，即之前的“开关”。理清楚设备、站点、仓位后，我们要知道，站点和仓位是在物理空间上不会移动、不会增删的，而设备是用来测试某个站点的某个仓位的数据的工具，这个工具是可以拆除移动，再安装到另一个站点的另一个仓位的。
