1. 低压监测服务对外开放的端口不用80
	1. 因为在linux和unix系统中，端口号1-1023是特权端口（系统端口），只有root权限的用户或者程序才可以绑定和监听这些端口。
	2. 所以如果应用程序监听80端口，应用程序就必须使用root身份在容器中运行， 存在安全风险。
	3. 这里我们选择10001
 - [ ] 后端查找cfg和dat文件的时候，是按照什么来规定两个文件属于同一个录波文件的。
	1. 结合[[字段说明]]，我认为系统应该根据file_name字段来判断。
```
	1. {
  "devices": [
    {
      "deviceId": "DEV003",
      "services": [
        {
          "serviceId": "fileTransfer",
          "data": {
            "fileInfo": {
              "totalFrames": 1,
              "curFrameIdx": 0,
              "fileContent": "",
              "fileFormat": "cfg",
              "fileMD5": "296bb6e4a767e9f7119f92fd351c92e5",
              "fileName": "2025-10-31_00-48-50.918.cfg"
            }
          },
          "eventTime": "20251119T102835Z"
        }
      ]
    }
  ]
}
```

1. 增加一个接入设备的功能，它表现为在设备列表页中刷新数据按钮的旁边多一个“接入设备”的按钮✅
	1. 用户点击这个按钮以后可以选择新设备的父节点（从已有的分组节点中选择）或者新建一个父节点（需要输入父节点名称——==站名==）;
	2. 然后输入新设备的==设备编码device_id==、==开关名称==name等信息;
	3. 另外，针对每个设备，我们新增一个字段叫做==开关额定电流==，这个字段也是用户在新建设备的时候需要填写的。
	4. 父节点——站名。eg：某某配电站
	5. 子节点——开关名称。eg：低压1号开关
	6. 实际修改：
		-  修改 device_info 表结构，添加 rated_current 字段✅
			- ALTER TABLE device_info ADD COLUMN rated_current DECIMAL(10,2) NULL COMMENT '额定电流';

		- 删除 device_group 表的 group_code 字段定义✅
		- 更新 SQL 建表脚本 create_sql_table.sql✅
2. 编辑设备、删除设备
	1. 编辑设备✅
	2. 删除时设计模拟量等表中外键✅
		1. 将外键的级联操作改为级联删除
3. 设备列表增加模糊搜索✅
4. 新建标签页——数据报表✅
5. 故障列表
	1. 后端新增接口，在waveform_file表中直接一次查询即可。（如果通过前端来遍历查询，要发送n次http请求）
	2. 
	git 