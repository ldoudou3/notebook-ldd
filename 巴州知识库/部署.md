# ollama模型部署
1. 用处：api_base_url = 'http://192.168.124.223:11434'  # ollama部署的文档解析embedding模型；
2. 部署流程：
	1. 登录221服务器：sqs @192.168.124.221 password
	2. Ollam的镜像就在路径：/mnt/hs-ssd/vllms/jisuan_backend
	3. 创建容器并启动：
```bash
		docker run -it --name ollama --gpus all --net host -v /mnt/hs-ssd/vllms/:/mnt/hs-ssd/vllms -v /mnt/hs-ssd/vllms/ollama:/root/.ollama ollama/ollama:0.9.6
```
	1. 